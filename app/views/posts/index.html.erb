<%- model_class = Post -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human.pluralize %></h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th>Icon</th>
      <th><%= model_class.human_attribute_name(:id) %></th>
      <th><%= model_class.human_attribute_name(:title) %></th>
      <th><%= model_class.human_attribute_name(:description) %></th>
      <th><%= model_class.human_attribute_name(:created_at) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @posts.each do |post| %>
      <tr>
        <td><i class="icon-signal"></i></td>
        <td><%= link_to post.id, post_path(post) %></td>
        <td><%= post.title %></td>
        <td><%= post.description %></td>
        <td><%=l post.created_at %></td>
        <td>
          <%= link_to t('.edit', :default => t("helpers.links.edit")),
                      edit_post_path(post), :class => 'btn btn-mini' %>
          <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                      post_path(post),
                      :method => :delete,
                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                      :class => 'btn btn-mini btn-danger' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
        <a class="btn btn-small btn-info" href="#" style="display:inline-block; float:left;"><i class="icon-info-sign icon-white"></i> More Info</a>
        <div class="btn-group">
          <a class="btn btn-primary" href="#" style="display:inline-block; float:left;"><i class="icon-user icon-white"></i> User</a>
          <a class="btn btn-primary dropdown-toggle" data-toggle="dropdown" href="#" style="display:inline-block; float:left;"><span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><a href="#" style="display:inline-block; float:left;"><i class="icon-pencil"></i> Edit</a></li>
            <li><a href="#" style="display:inline-block; float:left;"><i class="icon-trash"></i> Delete</a></li>
            <li><a href="#" style="display:inline-block; float:left;"><i class="icon-ban-circle"></i> Ban</a></li>
            <li class="divider" style="display:inline-block; float:left;"></li>
            <li><a href="#" style="display:inline-block; float:left;"><i class="i"></i> Make admin</a></li>
          </ul>
        </div>
 

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_post_path,
            :class => 'btn btn-primary', :style => 'display:inline-block; float:left;'%>  